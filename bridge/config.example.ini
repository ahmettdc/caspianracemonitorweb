; Caspian Live Bridge — yapılandırma
; Bu dosyayı "config.ini" olarak kopyala ve doldur (exe ile aynı klasörde).

[firebase]
; Public client değerleri (uygulamadaki firebase-config.js ile aynı) — hazır:
api_key = AIzaSyB9hEH26etwvn9adAGNOpPAlpUym1qzpns
database_url = https://caspian-race-control-default-rtdb.europe-west1.firebasedatabase.app
; EN KOLAY: exe'yi aç → "Google ile Giriş" → kendi hesabınla yayınla (bot GEREKMEZ).
; Giriş sonrası aşağıdaki iki satır otomatik yazılır (elle doldurma):
;   refresh_token = ...
;   google_email  = ...
; İSTEĞE BAĞLI bot yolu (arayüzsüz/CLI): bir Firebase Email/Password hesabı:
email = bridge-bot@caspian.local
password = DEGISTIR

[race]
; "Google ile Giriş" yaptıysan Takım/Yarış AÇILIR LİSTEDEN seçilir → burası otomatik dolar.
; (Elle: web'de yarışı açınca 'Canlı' sekmesindeki team_id/race_id.)
team_id =
race_id =

[rate]
; Saniyedeki gönderim (0.2–10). 2 önerilir — kota dostu, akıcı.
hz = 2
; LMU REST (VE% + gerçek takım adı/numara + hava). BAYRAK İÇİN GEREKMEZ (v2.2.4):
; bayraklar — sarı/FCY dahil — paylaşımlı bellekten okunur, REST kapalıyken de çalışır.
; Oyunun localhost
; sunucusuna istek atar; v1.4.131'den beri bu istekler ARKA PLAN thread'inde yapılır
; (okuma döngüsünü bloklamaz) → oyun donmaz. VE/takım adı istiyorsan true yap.
rest_on = false
; REST arka plan yenileme aralığı (sn). Varsayılan 3. VE yavaş değişir; hâlâ nadir
; takılma olursa 5-10 yap (daha seyrek istek).
rest_interval = 3
