; Caspian Live Bridge — yapılandırma
; Bu dosyayı "config.ini" olarak kopyala ve doldur (exe ile aynı klasörde).

[firebase]
; Public client değerleri (uygulamadaki firebase-config.js ile aynı) — hazır:
api_key = AIzaSyB9hEH26etwvn9adAGNOpPAlpUym1qzpns
database_url = https://caspian-race-control-default-rtdb.europe-west1.firebasedatabase.app
; Köprü için oluşturduğun BOT hesabı (Firebase'de Email/Password ile):
email = bridge-bot@caspian.local
password = DEGISTIR

[race]
; Web uygulamasında yarışı açınca URL/paylaşımdan aldığın kimlikler:
team_id =
race_id =

[rate]
; Saniyedeki gönderim (0.2–10). 2 önerilir — kota dostu, akıcı.
hz = 2
